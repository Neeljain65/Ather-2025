import React from "react";
import { FlipWords } from "../ui/flip-words";

export function FlipWordsDemo() {
  const words = ["HerSpark", "NSCETT", "NMTRC", "HackConquest "];

  return (
    
        <FlipWords words={words} /> 
      
  
  );
}
